#pragma once

const int NB_FREQ = 20000;                    // Number of sticks use.
const int SIZE_W = 1600;                    // Width of the window in pixels.
const int SIZE_H = 1000;                    // Height of the window in pixels.
const int MAX_FREQ = 1000;                  // Maximum frequence use in Hertz.
const int MIN_FREQ = 300;                   // Minimum frequence use in Hertz.
const int MIN_STICK_H = 20;                 // Minimal stick height.

const double SAMPLING_RATE = 44100.0;       // Sampling rate.
const int NB_CHANNEL = 2;                   // Number of channel.
const double AMPLITUDE = 0.9;               // Amplitude of the sin.